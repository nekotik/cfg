general {
	colors = true
	interval = 1
}

order += "memory"
order += "cpu_usage"
order += "tztime local"

cpu_usage {
	format = "CPU: %usage"
}

memory {
	format = "RAM: %used"
	threshold_degraded = "1G"
	format_degraded = "MEMORY < %available"
}

tztime local{
	format = "%Y-%m-%d %H:%M:%S"
}
